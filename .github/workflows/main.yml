on: [push]

jobs:
  demo:
    runs-on: ubuntu-latest
    name: Demonstration Board
    steps:
      - uses: actions/checkout@v4
      - uses: ./setup

      - id: preview-pcb
        uses: ./preview-pcb
      - id: schematic
        uses: ./schematic
      - id: erc
        uses: ./erc
      - id: drc
        uses: ./drc

      - uses: actions/upload-artifact@v4
        with:
          name: generated
          path: |
            ${{ steps.preview-pcb.outputs.output-top }}
            ${{ steps.preview-pcb.outputs.output-bottom }}
            ${{ steps.schematic.outputs.output }}
            ${{ steps.erc.outputs.output }}
            ${{ steps.drc.outputs.output }}
